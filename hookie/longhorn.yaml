# apiVersion: storage.k8s.io/v1
# kind: StorageClass
# metadata:
#   name: longhorn  # Name of the StorageClass
#   # Optional: You can add labels or annotations here if needed
# parameters:
#   numberOfReplicas: '3'  # Number of replicas for each volume
#   staleReplicaTimeout: '2880'  # Timeout for stale replicas
# provisioner: driver.longhorn.io  # Longhorn provisioner
# reclaimPolicy: Delete  # Automatically delete volume when PVC is deleted
# volumeBindingMode: Immediate  # Immediate binding to nodes for PVC
# allowVolumeExpansion: true  # Allow volume expansion for the PVC
