apiVersion: v1
kind: Service
metadata:
  name: snipeit-mysql-service
  namespace: snipeit-stack
spec:
  ports:
    - port: 3306
      protocol: TCP
      targetPort: 3306
  selector:
    app: snipeit-mysql
  type: ClusterIP
